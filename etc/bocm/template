LOWER=
VERSION=16.13.0

# Obowiazujaca funkcjonalnosc:

# 16.13.0
# Na podstawie wersji 16.2.1
# Katalog /etc/bocm jako repozytorium GIT

# 16.12.0
# 
# Poprawka w /etc/bocm/rsync_exclude katlog /var/lib/dhcp/*, potrzebny do poprawnego dzialania klienta dhcp

# 16.11.1
# Ostateczne kasowanie plików po starych kernelach linuxa
# Dodanie patcha /root/patch_grub-mkconfig dla grub-mkconfig w celu poprawnej obslugi overlayroot,w celu poprawnego dzialania update-grub nalezy ustawiac zmienna np. export GRUB_DEVICE=/dev/sda
# Poprawka zwiazana z pozyskiwaniem i zwalnianiem  przez dhcp adresu z sieci mfs
# Poprawka wskazywanie mfsmastera w initrd przez nazwe dns zamiast ip
# Aktualizacje pakietow Ubuntu

# 16.9.4mfschunk2
# Zbudowane obrazy dockera dla obliczen (bocompute) i mfschunka (bochunk)
# Zmieniony skrypt /etc/init.d/mfs, dostosowany do konfiguracji z kontenerem bochunk
# Zmieniony skrypt /etc/bocm/mfsconvert2, dostosowany do konfiguracji z kontenetem bochunk
# Zminiony plik /etc/bocm/functions.sh dostosowany do mfsconvert2

# 16.9.3
# Aktualizacja dystrybucji, zmiana domyslnego kernela
# Instalacja Docker-CE

# 16.9.2
# Dodanie banera /etc/update-motd.d/00-bo-header widocznego po zalogowaniu
# Poprawki zwiazane ze skryptem /usr/local/sbin/change_admin_address, ktory ustawia odpowiednie adresy hostname i uzytkownika

# 16.9.1
# Zabezpieczenie wywolan niektorych funkcji w skryptach poza initramfs-em, tych ktore sa przeznaczone do dzialania tylko podczas startu w srodowiku initramfs
# Zmiana nazw funkcji i skryptow z clustcontrol... na bocm...
# Uruchamianie funkcji bocm_top i bocm_bottom w bashu zamiast sh z initramfs-a
# Aktualizacja klienta mfs-a do wersji 3.0.100 pro
# Standardowa aktualizacja dystrybucji
#
# 16.9.0
# Eliminacja sciezek wpisywanych z palca, wszystkie sciezki przeniesione do zmiennych srodowiska
# Zmiana catalogu konfiguracji z /etc/clustcontrol na /etc/bocm "BlueOcean Config Manager"

# 16.8.X
# Dodano klienta MooseFS
# Podniesienie wersji systemu i pakietow, zabezpieczenie przed lukami bezpieczenstwa: Meltdown, Spectre
# Zmiana partycjonowania na lvm
# Zmiana sposobu uruchamiania z BIOS na EFI
# Dodanie obslugi vlanow na interfejsie mfs-a
# Wylaczenie pythona z initramfs-a (zmniejszenie rozmiaru)
# Dodanie funkcjonalnosci warstwy konfiguracji dla indywidualnego hosta CONFIGS/$hostname
# Dodanie parametrow kernela make_volumes, mfsmanualnet
# Dodanie parametrow domyslnych (plik default) DISKDEV, VG_NAME, VOLUMES_FILE, MANUAL_DISK_MANAGE
# Usuniecie parametrow domyslnych (plik default) CCSFDISKDEV, CCSFDISKFILE, CCINITIALIZE

